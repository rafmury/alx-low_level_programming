#include "main.h"

/**
 * mul - function that multiplies two numbers
 * @p: first number
 * @q: second number
 * Return: returns the result of multiplication of p and q
 */

int mul(int p, int q)
{
	return (p * q);
}
