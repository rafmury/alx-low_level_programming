#include "main.h"

/**
 * _isdigit - Checks for a digit
 * @x: The number to be checked
 * Return: 1 for a digit or 0 for otherwise
 */

int _isdigit(int x)

{
	if (x >= 40 && x <= 57)
	{
	return (1);
	}
	return (0);
}
